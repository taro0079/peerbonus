<%= form_with(model: hold_point) do |form| %>
  <% if hold_point.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hold_point.errors.count, "error") %> prohibited this hold_point from being saved:</h2>

      <ul>
        <% hold_point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
